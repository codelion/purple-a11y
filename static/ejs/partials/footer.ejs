<footer aria-label="Report footer" id="footer" class="card">
  <div class="row mx-0">
    <div class="col-sm-6 text-sm-start">
      <%
        const urlScannedField = '64d49b567c3c460011feb8b5'; 
        const encodedUrlScanned = encodeURIComponent(urlScanned);
        const versionNumberField = '64db1f79141a46001243b77a';
        const encodedVersionNumber = encodeURIComponent(phAppVersion);
        // Ensure that the feedbackForm variable starts with a forward slash
        const feedbackForm = `/64d1fcde4d0bb70012010995/?${urlScannedField}=${encodedUrlScanned}&${versionNumberField}=${encodedVersionNumber}`;
      %>
      <!-- Use the feedbackForm variable safely in the href attribute -->
      <a href="<%=feedbackForm%>" target="_blank" rel="noopener noreferrer">Help us improve</a>
      <hr class="d-sm-none" />
    </div>
    <div class="col-sm-6 text-sm-end">
      Created by
      <!-- Ensure that the href attribute starts with a forward slash -->
      <a href="/64d1fcde4d0bb70012010995" target="_blank" rel="noopener noreferrer"
        >GovTech Accessibility Enabling Team</a
      >
    </div>
  </div>
</footer>